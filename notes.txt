So our goal is to create a web server and along the way master the cpp language 
But before anything, I have 0 idea about how web servers work so lets learn about it first and then start building
So lets start with the basics 
What is socket?? Its is software endpoint that allows two programs to send and receive data across a network or the same computer 

now lets read "How does the Internet work?" - MDN
It gave an introduction to switches, routers, modems and why they are used, isps, difference between internet and web, 
ip address, domain name, intranet and extranet
It answers the question - "When I send a message from my laptop to another laptop, what physically happens?"

Read 2 Cloudflare Learning Center:  How does the Internet work? 
Defined packets and protocols, and learnt why packets and protocols are important
They also went into the steps of connecting to website that include DNS, TCP, TLS, HTTP request, and HTTP response
Went into detail in IP,DNS
Learnt about Ip more, what does a packet contain, ipv4 vs ipv6
HTTP, HTTP methods, HTTP Requests, HTTP headers, HTTPS reponse,     
DNS, DNS recursor, root nameserver,TLD nameserver, Authorative nameserver

Read3 
What is computer port?: ports defination, ICMP, firewall
MDN Client Server Overview: webserver, method, static and dynamic sites 

Read 4
Beej's Guide to Network Programming:sockets, raw socket, steam sockets, datagram sockets, tcp , udp- tftp dhp , tftp works with udp because of layering of 
protocols which sends ack packet , network theory, data encapsulation,ipv4 ipv6, subnets, port numbers, byte order- bigendian networkbyteorder - littleendian hostbyteorder
htons etc etc, struct - addrinfo; sockaddr;sockaddr_in;sin_zero;in_addr; sockaddr_storage, inet_pton()- make sure it is greater than zero before using,inet_ntop(), NAT
jumping from ipv4 to ipv6, System Calls- getaddrinfo() gai_strerror() for errors; freeaddrinfo();socket();bind(); connect();listen();accept(); send(); recv();
sendto(); receivefrom();close();shutdown();
Client-server: Its just the basics, I understand it now 
Now I am making a tcp echo server to understand this in depth 

So lets start server:
Firstly include/Socket.hpp contains the Socket class which will be used
src/Socket.cpp contains the actual implementation
include/Server.hpp deals with the concept of the server
src/Server.cpp implements the server behavior
src/main.cpp keep this short ,like create server, start server;
CMakeLists.txt Basically takes the source files and turns them into executable 
And as we know README for documentation but I am using notes.txt

This will be my flow 
I am stuck
     ↓
Think
     ↓
Check my notes
     ↓
Check Beej for concept
     ↓
Check man page for API details
     ↓
Compiler/error messages
     ↓
Search specific error
     ↓
Ask Chatgpt


Okay lets start with coding part
I was so indulged in writing socket.hpp, I forgot to update this

Okay I have reached a deadend and I am going to stop today there only for some reason I dont seem 
to convert const char array into a string and its pissing me off
Ohhh theres this new thing that I got to know so when we use angular brackets then the system looks into only the
standard libraries so if we want to use the local header files then its better to use double inverted comma

I finally understand the deadend and it was very simple , it was a type mismatch 
My program was expecting a pointer but I was passing it the object which was causing it the issue

Finally cleared the type issue and now I came accross a linker error 
/usr/bin/ld: /tmp/ccj8hThT.o: in function `main':
Socket.cpp:(.text+0xc6): undefined reference to `Socket::~Socket()'
/usr/bin/ld: /tmp/ccj8hThT.o: in function `Socket::Socket(std::__cxx11::basic_string<char, std::char_traits<char>, std::allocator<char> >, addrinfo const*, addrinfo*)':
Socket.cpp:(.text._ZN6SocketC2ENSt7__cxx1112basic_stringIcSt11char_traitsIcESaIcEEEPK8addrinfoPS6_[_ZN6SocketC5ENSt7__cxx1112basic_stringIcSt11char_traitsIcESaIcEEEPK8addrinfoPS6_]+0x75): undefined reference to `Socket::getaddrinfo(char const*, char const*, addrinfo const*, addrinfo**)'
/usr/bin/ld: Socket.cpp:(.text._ZN6SocketC2ENSt7__cxx1112basic_stringIcSt11char_traitsIcESaIcEEEPK8addrinfoPS6_[_ZN6SocketC5ENSt7__cxx1112basic_stringIcSt11char_traitsIcESaIcEEEPK8addrinfoPS6_]+0xf4): undefined reference to `Socket::socket(int, int, int)'
collect2: error: ld returned 1 exit status
Okay as far as I understand the error is caused because the getaddrinfo and the socketinfo already exists as global namespace
so instead of defining it, i think I should use them 

I was too confused about how to call getaddrinfo and socket in the linux api but just adding :: it is clear
Okay so the socket has been created , i have removed the getaddrinfo and socket functions that I was declaring as I was
innately using the global ones 

Okay so our next task is use to create the bind so that the socket created can be bound to 
a port which will be used for listening later 
I have no much idea about how to structure this so I am just going to write the bind inside the hpp
Or else lets try this 
Ill create a method to get back the fd 
I guess I was getting confused from line 85 but now I am clear, I will define the function inside the socket.hpp and then use them in socket.cpp

Okay so now created a bind function, although it used global namespaces that is linux api , but I just made error checking ig 
I am pretty confused whether I should include connect or now, so for now I will skip that part and try doing the listen , 
mostly as much as I know the connect is for the client side to make a connection with the server, tcp protocol, something like three way handshake
